node_modules/
.next/
playwright-report/
playwright-report-live/
test-results/
test-results-live/
.env*
!.env.example
*.log
*.tsbuildinfo
next-env.d.ts
.DS_Store
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.work/
.out/
.memory/

# Cached third-party resolve artifacts for an offline demo run.
# Never commit: these are transcripts of other people's videos.
.demo-cache/

# Local demo workdir with third-party media renders. Never commit.
.demo-final/

# Competitive intelligence store (scans, cache, memory)
.intel/

# Durable local product state (experiments and later creator-scoped stores)
.afterplay/

# Local Claude Code settings (per-developer, may contain permission overrides)
.claude/settings.local.json
