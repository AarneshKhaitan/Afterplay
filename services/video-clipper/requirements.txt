# Pin recent, and re-run `pip install -U yt-dlp` before any demo. YouTube changes how
# it signs media URLs every few weeks; an out-of-date extractor still reads metadata and
# captions fine but gets 403 on the video itself, which surfaces as an ffmpeg exit code
# and looks like a render bug. A 2026.07 build already failed this way against 2026.08.
yt-dlp>=2026.8.19
opencv-python>=4.9
numpy>=1.26
imageio-ffmpeg>=0.5
openai>=1.50
pytest>=8

# optional, each degrades cleanly when absent:
#   faster-whisper>=1.0   ASR for sources with no captions
#   anthropic>=0.40       LLM ranking, vision QC, copy generation  (--llm)
#   mcp[cli]>=1.2         MCP server transport
